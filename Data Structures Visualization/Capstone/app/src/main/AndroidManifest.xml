<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mitchelldennen.datastructurevisualization">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- activity android:name=".ArrayActivity" / -->
        <activity android:name=".CompareActivity" />

        <!-- ativity android:name=".ArrayAnimation" ></ativity -->

        <activity android:name=".AnimationActivity"></activity>
        <activity android:name=".AnimationActivityHash"></activity>
        <activity android:name=".AnimationActivityLL"></activity>
        <activity android:name=".AnimationActivityQueue"></activity>
        <activity android:name=".AnimationActivityStack"></activity>
        <activity android:name=".AnimationActivityTree"></activity>
        <activity android:name=".ViewActivity"></activity>
    </application>

</manifest>